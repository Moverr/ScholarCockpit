export const loggin =(payload)=>{
    return {
        type:"LOGIN_in",
        data:payload
    }
}

export const logout=(payload)=>{
    return {
        type:"LOGIN_in",
        data:payload
    }
}